package fr.vriege.anilib.feature.extensionrepository;

/** User-controlled activation state of an installed extension artifact. */
public enum ExtensionInstallationState {
    ENABLED,
    DISABLED
}
