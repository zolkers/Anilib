package fr.vriege.anilib.feature.source;

/** Filter controls compatible with the main Aniyomi source filter shapes. */
public enum SourceFilterType {
    HEADER,
    SEPARATOR,
    TEXT,
    CHECKBOX,
    TRI_STATE,
    SELECT,
    SORT
}
